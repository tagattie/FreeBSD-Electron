--- third_party/webrtc/system_wrappers/BUILD.gn.orig	2020-03-03 07:10:12 UTC
+++ third_party/webrtc/system_wrappers/BUILD.gn
@@ -58,8 +58,6 @@ rtc_library("system_wrappers") {
     if (!build_with_chromium) {
       deps += [ ":cpu_features_linux" ]
     }
-
-    libs += [ "rt" ]
   }
 
   if (is_win) {
