--- media/BUILD.gn.orig	2026-06-23 23:37:18 UTC
+++ media/BUILD.gn
@@ -102,6 +102,9 @@ config("media_config") {
       defines += [ "DLOPEN_PULSEAUDIO" ]
     }
   }
+  if (use_sndio) {
+    defines += [ "USE_SNDIO" ]
+  }
 }
 
 # Internal grouping of the configs necessary to support sub-folders having their
