--- third_party/webrtc/BUILD.gn.orig	2026-06-23 23:37:18 UTC
+++ third_party/webrtc/BUILD.gn
@@ -251,6 +251,9 @@ config("common_inherited_config") {
   if (is_linux || is_chromeos) {
     defines += [ "WEBRTC_LINUX" ]
   }
+  if (is_bsd) {
+    defines += [ "WEBRTC_BSD" ]
+  }
   if (is_mac) {
     defines += [ "WEBRTC_MAC" ]
   }
